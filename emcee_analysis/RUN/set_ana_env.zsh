#!/bin/zsh

export RUNDIR=/Users/daniellersch/Desktop/eta3Pi_DalitzAna/emcee_analysis
export PYTHONPATH=${RUNDIR}/:${PYTHONPATH}
export PYTHONPATH=${RUNDIR}/READ_ROOT_DATA/:${PYTHONPATH}
export PYTHONPATH=${RUNDIR}/UTILS/:${PYTHONPATH}
export PYTHONPATH=${RUNDIR}/EMCEE_FITTER/:${PYTHONPATH}
export PYTHONPATH=${RUNDIR}/FIT_UTILS/:${PYTHONPATH}
export PYTHONPATH=${RUNDIR}/SYSERR_ESTIMATION/:${PYTHONPATH}